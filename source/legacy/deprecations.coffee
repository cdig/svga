# This is sugar for Symbol developers, so that they don't have to update their takes right away

Take ["Animation","Mask"],
(      Animation , Mask)->
  
  Make "SVGAnimation", SVGAnimation = Animation
  Make "SVGMask", SVGMask = Mask
